# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets & auth (NEVER commit)
.env
.env.local
*.token
token.json
client_secret.json
credentials.json
uploader/tokens/
uploader/channels.json
uploader/*/tokens/
uploader/*/channels.json
uploader/*/history.json
docs/website-analytics-plan.md

# OS / editor
.DS_Store
.claude/*
!.claude/skills/

# Node
node_modules/

# Project data — large media / generated output / per-machine
/videos/
/products/
/music/
/supabase/

# Website product data — generated locally from /products/, committed so
# Vercel can build without access to the (gitignored) source product folders.
# Regenerate locally via `npm run prebuild` in website/ before committing.
# (intentionally NOT ignored: website/public/products/, website/public/products.json)
narration-benchmarking/
narration/voice-benchmarking/
assets/
uploader/metadata.json
uploader/history.json
uploader/youtube/stats/stats.csv
uploader/youtube/stats/comments.json

# Hyperframes pipeline — regenerated state, large external clones, junctions
hyperframes/_archive/
hyperframes/_inspiration/
hyperframes/projects/
hyperframes/rendered/
hyperframes/music
hyperframes/assets/
hyperframes/

# One-off / migration scripts (not part of the recurring pipeline)
hyperframes/scripts/update_plans.py

# Hedra starting-image benchmarking (local experiments, not pipeline)
hedra-starting-image-benchmarking/
same-face-benchmarking/
test-pinned-comment/
